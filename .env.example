PORT=3000

# PostgreSQL - use DATABASE_URL ou as variáveis separadas
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/arkmeds
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=arkmeds
# DB_USER=postgres
# DB_PASSWORD=postgres

NODE_ENV=development

# Base path para recibos (padrão: os.tmpdir())
# RECEIPT_BASE_PATH=

# Redis para fila BullMQ (notifier + worker de recibo). Se ausente, usa InMemoryRaceAcceptedNotifier.
REDIS_URL=redis://localhost:6379
